# Brands

Instructions to start the project:

*PREREQUISITES:* Node.js 10+ and `npm` (or `yarn`).

*INSTALL & RUN:*

```bash
npm install
```

```bash
npm run dev
# then open http://localhost:3000
```


If something fails, try deleting `node_modules` and running `npm install` again.

